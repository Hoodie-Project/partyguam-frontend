'use client';
import { Txt } from '@/components/_atoms';

import { TipBoxArea } from '../../_molecules/tipBox/TipBox';

const TipBox = Object.assign(TipBoxArea, {
  Txt: Txt,
});

export default TipBox;
